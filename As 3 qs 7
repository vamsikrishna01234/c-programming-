#include <stdio.h>

int main() {
    char str[100];
    int sum = 0;

    printf("Enter a string: ");
    scanf("%s", str);

    for(int i=0; str[i]!='\0'; i++) {
        sum += str[i];
    }

    printf("Sum of ASCII values of all characters in the given string: %d\n", sum);

    return 0;
}
