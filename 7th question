#include <stdio.h>
#include <string.h>

int main() {
    int num;
    char str[50];

    printf("Enter an integer: ");
    scanf("%d", &num);

    sprintf(str, "%d", num);

    printf("Number of digits: %ld", strlen(str));

    return 0;
}
